
<% @pages.each do |page| %>
  <div class="well">
    <h1><%= page.title %></h1>
    <div class="row">
      <%= link_to 'Edit', edit_page_path(page) %>
      <%= link_to 'Destroy', page, method: :delete %>
    </div>
    <%= page.body %>
  </div>
<% end %>

<br />

<%= link_to 'New Page', new_page_path %>
